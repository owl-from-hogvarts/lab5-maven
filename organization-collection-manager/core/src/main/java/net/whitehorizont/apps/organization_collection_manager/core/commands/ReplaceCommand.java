package net.whitehorizont.apps.organization_collection_manager.core.commands;

public class ReplaceCommand extends MetaCommand {
  public ReplaceCommand() {
    // create insert command instance
    // create remove command instance
    // add those command to internal queue (pass them to meta command constructor)
  }
}
